    import XCTest
    @testable import DripKit

    final class DripKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(DripKit().text, "Hello, World!")
        }
    }
